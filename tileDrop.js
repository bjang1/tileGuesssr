
    
        if (timer == oTime-4){
      tNum = floor(random(1,16));
      tiles[tNum].update();
      tiles[tNum].h = 0;
    }
        if (timer == 56) {
      tNum = floor(random(5,7));
      tiles[tNum].update();
      tiles[tNum].h = 0;
    }
    
     
    if (timer == 54) {
      tNum = floor(random(14,16));
      tiles[tNum].update();
      tiles[tNum].h = 0;
    }